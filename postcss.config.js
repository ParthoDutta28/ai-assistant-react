module.exports = {
  plugins: {
    tailwindcss: {}, // This tells PostCSS to use the Tailwind CSS plugin
    autoprefixer: {}, // This plugin automatically adds vendor prefixes (e.g., -webkit-, -moz-) to CSS rules
  },
}